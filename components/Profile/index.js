import React, { Component } from "react";
import { Text, Container } from "native-base";
class Profile extends Component {
  render() {
    return (
      <Container>
        <Text>We're in the profile</Text>
      </Container>
    );
  }
}

export default Profile;

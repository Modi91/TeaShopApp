import React, { Component } from "react";
import { Text, Container } from "native-base";
class OrderHistory extends Component {
  render() {
    return (
      <Container>
        <Text>We're in the history</Text>
      </Container>
    );
  }
}

export default OrderHistory;
